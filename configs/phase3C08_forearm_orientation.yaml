phase: 3C-0.8
target_directions:
  nearby_voxel_count: 5
  previous_reported_direction: [-0.77738, 0.62844, -0.02728]
native_grid:
  wrist_spacing_deg: 2.0
forearm:
  joint_name: forearm_PS
  diagnostic_range_deg: [-90.0, 90.0]
  coarse_spacing_deg: 5.0
  refinement_tolerance_deg: 0.25
augmented_grid:
  wrist_spacing_deg: 2.0
targeted_dynamics:
  maximum_trials: 50
  state_count: 10
  configurations_per_state: 5
  orientation_ramp_steps: 300
  transport_steps: 360
  command_increment_rad: 0.0017453292519943296
  forearm_actuator_kp: 10.0
  forearm_actuator_force_limit_n_m: 10.0
diagnostic:
  # TODO(PI): decide whether the diagnostic forearm/global-hand orientation DOF may become part of a later scientific model.
  # TODO(PI): if kinematics pass but dynamics remain zero, decide whether global hand translation may be studied next.
